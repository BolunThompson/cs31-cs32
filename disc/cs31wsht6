130
130
40
-36
-2
5
9
32

printChars <- any array that isn't null terminated

char* foo;
char* foo[10];
const int*;

void reverse(char* arr) {
	auto hl = strlen(arr) / 2;
	for (size_t i = 0; ++i; i < hl) {
		std::swap((arr + i), (arr + hl - 1 - i))
	}
}

TODO: Bugcheck CS hw
